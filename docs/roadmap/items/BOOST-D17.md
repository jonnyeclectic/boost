---
id: BOOST-D17
board: design
track: commands
status: done
impact: low
complexity: S
wow: 3
category: commands
ref: "commands/info.py:130–131"
order: 4
owner:
pr:
title: <code>list</code> polish — chips &amp; aligned counts
---
<code>list</code> already uses a nice <code>·</code> agent separator; take it further. Color the agent names as small chips, right-align the count footer with tabular figures, and add a dim header rule so the table reads as a designed object rather than raw columns.
