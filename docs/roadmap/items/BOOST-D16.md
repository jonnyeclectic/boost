---
id: BOOST-D16
board: design
track: commands
status: done
impact: med
complexity: M
wow: 4
category: commands
ref: "commands/info.py:177+"
order: 3
owner:
pr:
title: <code>info</code> skill identity card
---
<code>info</code> lists <code>kv()</code> pairs as flat dim labels. Promote it to a framed identity card: gradient skill name, status badges (<b>installed</b> / <b>pinned</b> / tap origin) styled like the web <code>.badge</code> pills, then the metadata — a real "detail view," not a key-value dump.
