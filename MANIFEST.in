# Keep source releases lean: the runtime is boost_cli/ (with its bundled
# data), plus the usual metadata. Everything below is dev-only or generated
# and should never ship in the sdist.
include README.md LICENSE pyproject.toml setup.cfg
recursive-include boost_cli *.py *.json
prune mutants
prune dist
prune .idea
prune .claude
prune .github
exclude .coverage
# The npm lint/test toolchain (stylelint, eslint, puppeteer-core, axe-core). CI
# only; nothing in the shipped runtime touches node.
exclude package.json package-lock.json
global-exclude __pycache__ *.pyc *.pyo .DS_Store
# integrations/ hosts separately-versioned distributions (boost-langchain);
# boost-skill-cli's own sdist must not ship a nested pyproject.
prune integrations
